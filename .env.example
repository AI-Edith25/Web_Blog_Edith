# Optional: lightly protects /studio with a single shared passcode,
# remembered via a cookie after first entry. Leave empty (or unset)
# to leave /studio completely open with no gate.
STUDIO_PASSCODE=
